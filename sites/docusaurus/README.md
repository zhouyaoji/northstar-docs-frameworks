# Docusaurus adapter

This adapter reads the shared files in `content/markdown` directly.

```bash
npm install
npm run build
```

The production build uses `/northstar-docs-frameworks/docusaurus/` as its base
URL so its assets work inside the combined GitHub Pages deployment.
