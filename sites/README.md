# Site adapters

Each directory contains only the configuration, theme overrides, and build
scripts required by its generator. Shared prose belongs under `content/`.

Each active adapter README records its source format and public path. Placeholder
adapters document experiments that have not yet joined the Pages build.
