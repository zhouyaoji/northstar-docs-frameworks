# Site adapters

Each directory will contain only the configuration, theme overrides, and build
scripts required by its generator. Shared prose belongs under `content/`.

Until the build strategy is implemented, each adapter README records its source
format and intended public path.
