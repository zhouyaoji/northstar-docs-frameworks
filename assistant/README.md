# Planned documentation assistant

This directory is reserved for a framework-neutral assistant that reads
approved AIPP objects directly. It will eventually contain retrieval logic, an
API, a shared browser widget, framework adapters, and evaluation questions.

The assistant is deferred because static GitHub Pages cannot securely run a
model-backed API. No credentials or model calls should be placed in client-side
site code.
