# Generators

Future conversion tools will transform either the shared Markdown source or the
planned AIPP canonical objects into the markup variants under `content/`.

The first generator milestone should preserve stable page IDs, headings, links,
code blocks, tables, and source attribution.
