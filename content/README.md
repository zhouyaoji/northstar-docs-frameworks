# Content formats

`markdown/` contains the initial shared Northstar content. The remaining
directories reserve a home for equivalent content in formats used by the site
adapters.

Generated files will eventually carry a header identifying their source page
ID and should not be edited directly. `manifest.yaml` defines those stable IDs
independently of filenames or framework routes.
