# Planned AIPP integration

This directory is reserved for canonical AI Publication Protocol objects,
lifecycle state, discovery metadata, and feeds.

Implementation is deferred until the initial documentation sites build and
deploy reliably. The intended source project is
[`zhouyaoji/aipp-demo`](https://github.com/zhouyaoji/aipp-demo).
